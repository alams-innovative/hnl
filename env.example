# Copy this file to `.env.local` (recommended) or `.env` and fill values.
#
# Required for DB-backed routes (admin dashboard + form submissions)
# You can use Neon or any Postgres instance.
DATABASE_URL="postgres://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require"

# Recommended (admin session cookie signing). If unset, a default dev-only secret is used.
SESSION_SECRET="change-me-to-a-long-random-string"

# Optional: Google reCAPTCHA v3
# If unset, captcha is bypassed in development and forms still submit.
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=""
RECAPTCHA_SECRET_KEY=""

# Optional: AI Assistant (used by /api/ai-assistant). Without this, the endpoint returns a fallback response.
OPENAI_API_KEY=""


